{
	"name": "quiz-watch-data",
	"version": "1.0.0",
	"devDependencies": {
		"gh-pages": "^2.2.0",
		"parcel-bundler": "^1.12.4"
	},
	"scripts": {
		"build": "rm -rf ./dist && parcel build index.html --public-url ./",
		"deploy": "yarn build && gh-pages -d dist",
		"start": "parcel index.html"
	}
}
